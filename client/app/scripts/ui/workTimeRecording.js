'use strict';

var WorkTimeRecordBox = React.createClass({
    render: function () {
        return (
            <div>
                hi
            </div>
        );
    }
});

React.render(<WorkTimeRecordBox />, document.getElementById('recording'));